import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wearezipline.demo',
  appName: 'AutoFill Test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;

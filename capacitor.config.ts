import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AutoFill Test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
